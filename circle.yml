dependencies:
  cache_directories:
    - ~/.npm
  pre:
    - rm -Rf ./node_modules
  override:
    - nvm install && nvm alias default $(cat .nvmrc)
    - npm install

test:
  override:
    - npm test
